# التسليم والنشر (Windows)

## 1) ابنِ التطبيق
```bat
pyinstaller packaging\showroom_erp.spec
```
الناتج: `dist\ShowroomERP\` (مجلد تشغيل كامل).

## 2) مُثبّت بنقرة واحدة (اختياري لكن مُوصى به)
1. ثبّت [Inno Setup](https://jrsoftware.org/isinfo.php).
2. افتح `packaging\installer.iss` ثم **Build**.
3. الناتج: `ShowroomERP_Setup_1.0.0.exe` — هو ما تسلّمه للعميل.

## 3) ما الذي تسلّمه؟
- **إمّا** المُثبّت `ShowroomERP_Setup_*.exe` (الأفضل للعملاء).
- **أو** مجلد `dist\ShowroomERP\` كاملًا مضغوطًا (نسخة محمولة Portable).

## 3.1) سير العمل: ابنِ على اللاب توب → فلاشة → أجهزة المعارض
1. على اللاب توب: `pyinstaller packaging\showroom_erp.spec`.
2. انسخ مجلد **`dist\ShowroomERP\` كاملًا** إلى الفلاشة (يحوي `.exe` + `_internal`
   بكل القوالب والأيقونة والخط — لا يعتمد على أي ملف خارجي).
3. على جهاز المعرض: انسخ المجلد من الفلاشة إلى `C:\ShowroomERP\` مثلًا، وشغّل
   `ShowroomERP.exe` (لا يحتاج تثبيت Python). أو شغّل المُثبّت `Setup.exe`.
4. أول تشغيل على جهاز المعرض ينشئ قاعدة بيانات جديدة في
   `%LOCALAPPDATA%\ShowroomERP` ويطلب الإعداد الأوّلي.

> **نقل بيانات معرض موجود لجهاز جديد:** من البرنامج على الجهاز القديم اعمل
> «نسخة احتياطية»، انسخ ملف `.db` إلى الجهاز الجديد، ثم من البرنامج هناك اعمل
> «استعادة من ملف». تُطبَّق الترقيات تلقائيًا إن كان الإصدار أحدث (يحافظ على
> البيانات).

## 4) قابلية البيع لمعارض متعددة
- كل معرض = نسخة مستقلة؛ بياناته معزولة في `%LOCALAPPDATA%\ShowroomERP`.
- التخصيص (اسم المعرض/القالب) من داخل **الإعدادات** بلا إعادة بناء.
- الترقية بين الإصدارات تعتمد نظام Migrations في `app/data/migrations.py`
  (يحافظ على بيانات العميل عند التحديث).

## 5) قبل التسليم — تحقّق سريع
- شغّل `ShowroomERP.exe`، أكمل الإعداد الأوّلي، وتنقّل بين الوحدات.
- جرّب: بيع نقدي/آجل، فاتورة شراء، تحصيل قسط، تقرير، نسخة احتياطية + استعادة.
- (اختياري) أضف خط Cairo قبل البناء لمظهر عربي أنيق.
