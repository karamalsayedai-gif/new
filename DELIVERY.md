# التسليم النهائي — نظام إدارة معرض الدراجات النارية (RC1)

الإصدار: **1.0.0-rc1** · المنصّة: **Windows 64-bit** · بلا اتصال · عربي RTL.

---

## 1) حزمة التسليم النهائية

### مسار ملف التشغيل
```
dist\ShowroomERP\ShowroomERP.exe
```

### الملفات/المجلدات التي تُنسخ على الفلاشة (كاملة)
انسخ **مجلد `dist\ShowroomERP\` بالكامل** كما هو:
```
dist\ShowroomERP\
├── ShowroomERP.exe          ← ملف التشغيل
└── _internal\               ← لا تحذفه ولا تعدّله
    ├── app\theme\themes\     (القوالب الأربعة)
    ├── app\assets\icons\app.ico
    ├── app\assets\fonts\Cairo-Regular.ttf
    └── (مكتبات Qt/Python)
```

### ما يُنسخ كاملًا لجهاز العميل
- **مجلد `ShowroomERP` بأكمله** (الـ exe + مجلد `_internal`). لا يعمل الـ exe بدون `_internal`.

### ما الذي **لا** يجب حذفه من `dist\ShowroomERP`
- لا تحذف مجلد **`_internal`** ولا أيًّا من محتوياته (يحوي القوالب والخط والأيقونة ومكتبات التشغيل).
- لا تحذف **`ShowroomERP.exe`**.

---

## 2) خطوات التسليم لصاحب المعرض (بسيطة جدًا)
1. انسخ مجلد **ShowroomERP** بالكامل من الفلاشة إلى الجهاز (مثلًا `C:\ShowroomERP`).
2. افتح **`ShowroomERP.exe`**.
3. أول تشغيل: أدخل **اسم المعرض** وأنشئ **حساب المدير** (اسم مستخدم + كلمة مرور).
4. قاعدة البيانات تُنشأ تلقائيًا في مجلد بيانات ويندوز (لا تحتاج فعل شيء).
5. من داخل البرنامج: افتح **النسخ الاحتياطي** وفعّل النسخ التلقائي أو خذ نسخة يدويًا بين الحين والآخر.

---

## 3) Admin handoff (صاحب المعرض / المدير)
- **بيانات الدخول الافتراضية:** لا يوجد حساب افتراضي. أول تشغيل يطلب إنشاء حساب المدير بنفسك (أكثر أمانًا).
- **أول مرة:** أدخل اسم المعرض + حساب المدير → ستدخل مباشرة على لوحة التحكم.
- **تغيير الإعدادات:** تبويب **الإعدادات** → بيانات المعرض، القالب (4 قوالب)، قالب الطباعة (ترويسة/تذييل)، الأمان (عدد محاولات الدخول ومدة القفل)، بادئات ترقيم الفواتير.
- **نسخة احتياطية:** تبويب **النسخ الاحتياطي** → «نسخ احتياطي الآن» أو فعّل «تلقائي»/«عند الإغلاق». تُحفظ النسخ في مجلد بيانات ويندوز، ويمكن «نسخ إلى مسار…» لحفظها على فلاشة.
- **نقل البيانات لجهاز آخر:** على الجهاز القديم: نسخة احتياطية → انسخ ملف `.db` لفلاشة. على الجهاز الجديد: ثبّت البرنامج → **النسخ الاحتياطي** → «استعادة من ملف» (يأخذ نسخة أمان تلقائيًا قبل الاستبدال ويطبّق الترقيات).
- **تحذير SmartScreen (إن ظهر):** لأن الملف غير موقّع رقميًا قد يظهر «Windows protected your PC». اضغط **More info → Run anyway**. (يُحلّ نهائيًا بشراء توقيع رقمي مستقبلًا.)

---

## 4) Developer handoff (المطوّر)
- **ملف البناء الرسمي:** `packaging/showroom_erp.spec` (الوحيد المعتمد).
- **ملفات الثيمات:** `app/theme/themes/<theme>/` (لكل قالب: `manifest.json`, `colors.json`, `metrics.json`, `typography.json`, `main.qss`). محرّك القوالب: `app/theme/theme_manager.py`.
- **الخطوط:** `app/assets/fonts/Cairo-Regular.ttf` (+ `OFL.txt`). التحميل: `app/ui/branding.py`.
- **الأيقونة:** `app/assets/icons/app.ico`.
- **وثائق:** `BUILD.md` (البناء) · `RUN.md` (التشغيل المحلي) · `PACKAGE.md` (التسليم/الفلاشة) · `docs/ARCHITECTURE.md`.
- **نقطة الدخول:** `main.py` → `app/application.py` (الإقلاع، الثيم، التنقّل).
- **قاعدة البيانات والنسخ على ويندوز:**
  ```
  %LOCALAPPDATA%\ShowroomERP\showroom.db
  %LOCALAPPDATA%\ShowroomERP\backups\
  ```
- **المسارات الآمنة للتغليف:** `app/config.py: resource_path()` (يتعامل مع `sys._MEIPASS`).
- **الترقيات:** `app/data/migrations.py` (الإصدار الحالي للمخطط: 9؛ مُختبَر v1→v9 مع حفظ البيانات).

---

## 5) Known limitations (القيود الحالية المهمة فقط)
- **توقيع رقمي:** الـ exe غير موقّع → قد يظهر تحذير SmartScreen (يُتجاوز يدويًا).
- **ترقيم الفواتير:** تسلسلي عام بِبادئة قابلة للضبط (وليس تسلسلًا سنويًا/شهريًا منفصلًا).
- **بناء ويندوز:** يجب توليد الـ exe على جهاز ويندوز (PyInstaller يبني لنظام التشغيل المضيف). الـ spec مُتحقَّق منه ببناء وتشغيل كاملين.
- **النسخ الاحتياطي محلي:** لا يوجد نسخ سحابي تلقائي (يدوي إلى فلاشة/مسار).

---

## 6) ملخص التسليم (مرجع سريع)
- نظام ERP كامل لمعرض دراجات: مخزون، مشتريات، موردون، عملاء، مبيعات (نقدي/آجل/جزئي)، تقسيط، خزينة، مصروفات/إيرادات، **مرتجعات بيع/شراء**، إقفال يومي، تقارير (9 شاشات + **مخططات**)، مستخدمون وصلاحيات، **سجل تدقيق**، نسخ احتياطي/استعادة، إعدادات.
- 4 قوالب قابلة للتبديل، خط Cairo مرفق، تنقّل صفحات كاملة، ترقيم فواتير رسمي، قفل دخول بعد محاولات، قوالب طباعة قابلة للتخصيص.
- متكامل ماليًا: كل عملية تربط المخزون + الخزينة + حساب العميل/المورّد + اليوم المفتوح + التدقيق، ذرّيًا.
- جاهز للبناء بـ PyInstaller والنشر عبر فلاشة على أجهزة المعارض.
